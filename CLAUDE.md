# Virtual Silicon Lab — Project Memory

A web creative tool for designing fictional semiconductor chips on a 2D canvas and exporting
high-resolution chip images and keynote-style promo posters. **Not** an EDA tool, not a game,
no real manufacturing. Tone: surreal / Sci-Fi / playful (fantasy blocks like "Consciousness
Processor" + funny fake spec sheets). Audience: people who love chip aesthetics, not EE pros.

**Visual quality IS the product.** If glow/neon/metal/export looks amateurish, the project fails.
Completion and "looks great at a glance" beat feature breadth.

## Working Context

- The v1 MVP (M0–M6), the v2 visual major (V2-M0–M6), the SoC Custom Studio work, and **v3 "Share Core" (V3-M0–M6)** are all complete and live on `main`.
- **v4 "Community"** is complete on branch `v4-community` through **V4-M4 (remix lineage)**, not yet merged. Completed milestones: **V4-M0 (moderation + access gate)**, **V4-M1 (reactions: likes + comments + report button)**, **V4-M2 (ranking/trending)**, **V4-M3 (contests)**, and **V4-M4 (remix lineage)**. Roadmap: `docs/superpowers/specs/2026-06-12-v3-v4-roadmap-design.md`. Public launch is a separate gate; the access gate (`VSL_SIGNUPS_OPEN`, default `false`) is the mechanism for it.
- **v5 "Public Launch"** is complete through the pre-production launch checkpoint on branch `v5-public-launch`: invite access, safety/account/onboarding/discovery/ops hardening, end-to-end launch-flow test, green gates, runbook, and QA checklist. The production gate is **not live yet**; `VSL_ACCESS_MODE=invite` requires explicit owner go/no-go after manual browser QA and backup sign-off.
- **v6 "Mobile/Responsive"** is complete through **V6-M4** on branch `v6-mobile-responsive`, not yet merged: a 768px responsive pass (drawer nav + single-column reflow) over the public read surfaces, account/dashboard, and a mobile read-only editor preview, capped by a 360/390px QA matrix and visual-gate sign-off (`docs/ops/mobile-qa.md`). No schema/migration/API change; admin stays desktop-only and out of scope; public launch remains a separate gate. Spec: `docs/superpowers/specs/2026-06-17-v6-mobile-responsive-design.md`.
- **v7 "Visual Depth"** is complete through **V7-M6** on branch `v7-visual-depth` (not yet merged); **V7-M0–M6 are done (M4 intentionally dropped)**, version line at **0.5 v7**, branch left unmerged pending the owner's integration/PR + public-launch gate. The editor and public gallery share a lazy full-screen Three.js showcase derived from the same project snapshot, with PBR/PMREM/ACES/bloom, turntable/glow, editor-only deterministic MP4 export, and a 400-piece/WebGL admission budget that falls back to the poster. The server-rendered share viewer links to gallery 3D without client JS. Three, recorder, and `mp4-muxer` stay in lazy chunks; no schema/migration/API/upload change. M5 spec: `docs/superpowers/specs/2026-06-19-v7-m5-gallery-share-integration-design.md`.
- **v8 "AI-Assisted Creation"** is in progress on branch `v8-ai-assisted-creation` through **M1 (AI naming + fake-spec copy)**, not yet merged. **M0 (server AI foundation)** built a pure draft-to-`Project` mapping, an `ai_prompt_log` table, and an authenticated `POST /api/ai/generate-draft` endpoint behind a per-user daily quota, with no user-facing UI. **M1** adds the first user-facing AI feature: a zero-input "Generate from this chip" control that derives chip context client-side, calls `POST /api/ai/generate-copy` (reusing M0's quota/log/provider plumbing, `kind='generate-copy'`, no schema/migration change), and previews the result with Apply (via the existing `setSpec` editor command, one undoable commit) / Discard. `ANTHROPIC_API_KEY` is server-only and never reaches the client bundle; local-first editing and the publish/export contracts are unchanged. Interactive browser QA of the generate→preview→Apply flow is owner-manual/pending.
- v3 added a Node + TypeScript + SQLite backend (npm workspace `server/`) with accounts, publish-snapshot uploads, a public gallery, share links, and remix import. Editing stays 100% local-first (IndexedDB + localStorage fallback); the server only receives explicit publish snapshots. v3 ended at **deploy-ready**, not public launch.
- Package manager: **npm**.
- Node.js `20.19+` or `22.12+` (Vite/Vitest requirement).

## Commands

```bash
npm test          # client suite first, then server suite (server skipped if client fails)
npm run test:client   # client vitest only
npm run build     # tsc -b && vite build
npm run dev -- --host 127.0.0.1   # client dev server for browser verification
npm run dev:server               # API server on http://127.0.0.1:8787
npm run typecheck --workspace server   # server typecheck (tsc --noEmit)
```

Run `npm test` and `npm run build` after every task. Exercise the milestone acceptance gate in a
browser after frontend changes, then record decisions in `implementation.md`.

## Tech Stack

Vite · React + TypeScript · Tailwind CSS v4 · Zustand (`zustand/vanilla` + `useStore`) ·
Konva + React Konva · `idb` (IndexedDB) · Vitest + React Testing Library + `fake-indexeddb`.
Server (`server/` workspace): Hono + `@hono/node-server` · better-sqlite3 (no ORM) · tsx dev runtime.

## Architecture & Boundaries

```text
src/
  app/        routing, application shell
  domain/     versioned project JSON, factories, migrations  (NO React/Konva/Zustand/IndexedDB)
  storage/    IndexedDB + localStorage repositories (persist/migrate only; no editor behavior)
  stores/     Zustand state + editor commands (duplicate, delete, reorder, undo, redo)
  features/
    projects/ project dashboard
    editor/   editor shell, toolbar, keyboard shortcuts
      canvas/ Konva stage, die, blocks, decorations, geometry
    specs/    fake spec form + preview          (Milestone 5)
    export/   die-only + poster export stages   (Milestone 5)
    landing/  portfolio landing page            (Milestone 6)
  presets/    curated remixable chip definitions (Milestone 4)
  themes/     Konva-renderable visual tokens     (Milestone 3)
  visual/     v2 page themes, material recipes, hero sets, random generator
    chip3d/   pure serializable 2D-to-3D derivation (NO Three.js imports)
  three/      lazy Three.js renderer and scene lifecycle
  lib/        framework-agnostic utilities (zero deps; e.g. debouncer)
  test/       test setup (fake-indexeddb + jest-dom)
server/       npm workspace `@vsl/server` (v3 Share Core backend)
  src/        Hono app, SQLite open/migration runner, entry point
  test/       node-environment Vitest suite
```

Rules:
- `src/domain/` is pure: no React/Konva/Zustand/IndexedDB/browser imports.
- The server reuses `src/domain/` via the `@domain/*` alias (tsconfig paths + vitest alias) and must not import from any other client directory.
- Canvas components receive serializable project data and emit domain-level changes.
- Export stages receive the same serializable data and **must not scrape editor DOM**; they composite on a dedicated Konva stage.
- Every persisted schema change bumps `schemaVersion`, adds a migration test, and is noted in `implementation.md`.

## Conventions

- **TDD**: write a failing test, confirm it fails, implement minimal code, confirm pass, commit. One concern per commit.
- Vitest with explicit `import { describe, expect, it } from 'vitest'` (no globals).
- Vanilla Zustand stores are tested through `store.getState()`.
- Pure logic (geometry, factories, store commands, debounce, shortcuts, zoom math) is unit-tested directly.
- **Konva rendering is NOT unit-tested** (jsdom lacks canvas); it is verified in a browser session. Keep pure helpers out of components so they stay testable.
- Serializable project JSON is the single source of truth (single-JSON export shape from day one).

## Key Product Invariants

- Four die shapes: `rect`, `square`, `circle`, `hexagon`. Blocks are clamped inside die bounds on move AND resize.
- Export visual effects use **Konva node settings** (`shadowBlur`, gradients, filters, blend) — never DOM/CSS, which `toDataURL()` ignores. DOM/CSS effects are editor-UI-only.
- Two PNG exports: `die-only` and `poster` (poster composited on a separate export-only Konva stage).
- v2 poster formats: `press-hero`, `architecture-slide`, `product-closeup`; poster raster remains `3200x1800`.
- Desktop-first (Chrome); responsive is post-MVP. Autosave is required; target ~60fps canvas.
- v1 editor: single selection; multi-select deferred.

## Milestone Status

### v8 AI-Assisted Creation (in progress — branch `v8-ai-assisted-creation`, not yet merged)

- **V8-M0 Server AI Foundation**: ✅ done — server-only foundation, no client UI. Pure `src/domain/ai/` (`AiChipDraft` type + `mapAiDraftToProject(draft, id?, now?)`) maps any AI draft to a domain-valid `Project`: drops unknown block types, clamps blocks into the die's rectangular bounding box, reassigns sequential z-order, and keeps `schemaVersion` at 5 — this single pure mapping is the valid-project guarantee regardless of which provider produced the draft. `013_ai` migration adds an append-only `ai_prompt_log(id, user_id, kind, prompt, created_at)` table (CASCADE on user delete), no `Project` schemaVersion change. `server/src/ai/` adds `resolveAiConfig` (env: `VSL_AI_PROVIDER` default `fake`, `VSL_AI_MODEL` default `claude-opus-4-8`, `VSL_AI_DAILY_QUOTA` default `20`, `ANTHROPIC_API_KEY` server-only), an `AiProvider` interface with a deterministic `createFakeProvider`, and a thin real `createAnthropicProvider` (`@anthropic-ai/sdk`, `output_config.format` json_schema) — structured outputs enforce shape only, so every invariant (clamp/z-order/schemaVersion) still flows through the factory mapping for either provider. Per-user 24h quota (`countRecentGenerations`/`logPrompt`, logged before the provider call so failed/abusive calls still count) gates the authenticated `POST /api/ai/generate-draft` (401/400/429/503/200, returns an UNSAVED draft `Project`); `buildAppDeps` selects the Anthropic provider only when `VSL_AI_PROVIDER=anthropic` AND the key is set, else fake. Key-leak check confirmed `ANTHROPIC_API_KEY` is absent from the client `dist/assets` bundle. Final gates: client 90 files/459 tests, server 68 files/256 tests, build/server typecheck/lint green. Spec: `docs/superpowers/specs/2026-06-19-v8-m0-server-ai-foundation-design.md`; plan: `docs/superpowers/plans/2026-06-19-v8-m0-server-ai-foundation.md`.
- **V8-M1 AI Naming + Fake-Spec Copy**: ✅ done — the first user-facing AI feature, zero-input ("Generate from this chip" — no prompt textarea). Pure `src/domain/ai/deriveAiChipContext.ts` derives a minimal `AiChipContext` (`theme`/`dieShape`/`blockTypes`/optional `name`) from the live `Project`; pure `src/domain/ai/mapAiSpecDraftToFakeSpec.ts` is the M1 mirror of M0's valid-project guarantee — it maps any `AiSpecDraft` to a domain-valid `FakeSpec` (string trim + length caps, `cores` clamped to a non-negative integer ≤4096, `features` bounded to 6 trimmed non-empty strings, missing/wrong-typed fields fall back to fixed defaults), so adversarial AI copy can never produce an invalid spec. `AiProvider` gains `generateSpecCopy` (deterministic fake; `createAnthropicProvider` reuses `claude-opus-4-8` + `output_config.format` json_schema). `POST /api/ai/generate-copy` reuses M0's auth + **shared** 24h quota (`kind='generate-copy'`, same `ai_prompt_log` table, **no new migration**; a new shared `requireUserWithinQuota` helper now backs both generate-draft and generate-copy) and returns `{ spec: FakeSpec }` (401/400/429/503/200, 400 on missing/invalid context), writing nothing to `projects`/`published_chips`. Client `aiCopyApi` mirrors the publish API's error mapping (offline/502-504 → `AiServerUnreachableError`, `{error}` body → typed `AiApiError`); the new additive, lazy `AiSpecPanel` mounts above `FakeSpecForm` in `EditorInspectorRail` with a generate → preview → Apply (`setSpec`, one undoable commit) / Discard flow; every failure path renders an inline message without disabling manual spec editing. No `Project`/`FakeSpec` schema change, no new editor command, local-first untouched. Final gates: client 94 files/475 tests, server 68 files/264 tests, build/server typecheck/lint green; key-leak check re-confirmed clean. Interactive browser QA of the generate→preview→Apply flow is **owner-manual/pending** — evidence at this checkpoint is the automated gates plus fake-provider route/panel tests. Spec: `docs/superpowers/specs/2026-06-19-v8-m1-ai-naming-spec-copy-design.md`; plan: `docs/superpowers/plans/2026-06-19-v8-m1-ai-naming-spec-copy.md`.

### v7 Visual Depth (✅ complete through M6 — branch `v7-visual-depth`, not yet merged)

- **V7-M0 3D Showcase Foundation**: ✅ done — pure `Chip3DModel` derivation from `ChipLayerModel`; Three.js isolated behind a lazy `src/three/` chunk; package/die/block extrusion with flat M0 colors; change-driven OrbitControls + reset; DPR cap, resize handling, and geometry/material/renderer/context disposal; desktop editor `Open 3D showcase` full-screen modal with Escape, focus restoration, WebGL/error fallback. Browser QA passed on the actual 12-block N1 GREEN HORIZON project. Final gates: client 83 files/426 tests, server 62 files/242 tests, build/server typecheck/lint green. Plan: `docs/superpowers/plans/2026-06-18-v7-m0-3d-showcase-foundation.md`.
- **V7-M1 3D Material & Lighting**: ✅ done — lifts the M0 flat-box showcase to the v2/v3 premium bar. New pure `resolveChip3DStyle(theme)` (`src/visual/chip3d/chip3dMaterials.ts`) maps `resolveMaterialRecipe`/`resolveTheme` → serializable PBR descriptors (`Chip3DMaterial`/`Chip3DEnvironment`/`Chip3DStyle`); `Chip3DModel` pieces now carry a `material` (flat `color` removed) and the model carries an `environment`; `buildChip3DModel(layers, die, style)` replaces the M0 palette arg (`Chip3DPalette` deleted). `src/three/` builds `MeshStandardMaterial` per piece, sets `scene.environment` from a procedural PMREM gradient (no HDRI asset), applies ACES tone mapping + a three-point rig, and renders through an `EffectComposer` + `UnrealBloomPass` (postprocessing/PMREM stay in the lazy `Chip3DViewer-*` chunk, no new npm dep). Material/bloom constants are centralized in the pure mapper; browser QA tuned the emissive band (~1.0) + bloom so neon fantasy blocks glow without blowing out. Owner visual-quality sign-off recorded (N1 GREEN HORIZON neon + LUCID MONO PACKAGE). Final gates: client 84 files/431 tests, server 62 files/242 tests, build/server typecheck/lint green; lazy viewer chunk 580.13 kB (gzip 147), core `index-*` free of three. Spec: `docs/superpowers/specs/2026-06-18-v7-m1-3d-material-lighting-design.md`; plan: `docs/superpowers/plans/2026-06-18-v7-m1-3d-material-lighting.md`.
- **V7-M2 Turntable & Animation**: ✅ done — pure deterministic `turntableAzimuthAt`/`glowPulseAt` functions with centralized 14s turntable + 3s `[0.8,1.2]` pulse defaults; showcase opens paused and the in-viewer Play/Pause control starts/stops the only rAF loop; current camera offset capture, controls disable/re-sync, base emissive restore, reset handling, and cleanup avoid jumps/leaks. Browser QA passed N1 GREEN HORIZON plus no-fantasy mono PANTHER SCALE; 180-frame measurement worst 9.3ms/p95 9.1ms. Final gates: client 85 files/438 tests, server 62 files/242 tests, build/server typecheck/lint green; lazy viewer 581.41 kB (gzip 147.56), core remains free of Three. Spec: `docs/superpowers/specs/2026-06-18-v7-m2-turntable-animation-design.md`; plan: `docs/superpowers/plans/2026-06-18-v7-m2-turntable-animation.md`.
- **V7-M3 MP4 Export**: ✅ done — pure fixed-timestep 240-frame capture spec reuses M2 time functions for one seamless rotation + three glow cycles; shared `chip3dStage` keeps live/offscreen lighting and PMREM identical; lazy WebCodecs H.264 + `mp4-muxer` encoder and offscreen Three recorder produce an opaque 1280×720/30fps/8s MP4. Showcase panel reports progress/errors/unsupported browsers and dynamically imports the recorder. Browser QA decoded an actual 3.79 MB N1 GREEN HORIZON output at exact dimensions/duration with matching PBR+bloom; Play/Pause/Reset stayed green. Final gates: client 87 files/447 tests, server 62 files/242 tests, build/server typecheck/lint green; Three and muxer remain lazy. Spec: `docs/superpowers/specs/2026-06-18-v7-m3-mp4-export-design.md`; plan: `docs/superpowers/plans/2026-06-18-v7-m3-mp4-export.md`.
- **V7-M4 Shader-grade 2D Enhancement**: ✅ dropped (recorded decision) — optional shader-grade 2D enhancement not built; M1–M3 closed the 2D/3D gap and any export-visible effect would need Konva re-implementation. PixiJS was rejected with no new dependency. Recorded in the M5 spec's `v7-M4 disposition` and `implementation.md`.
- **V7-M5 Gallery / Share Integration + Performance Budget**: ✅ done — shared `Chip3DShowcase` extracted from the editor toggle; gallery detail gains a lazy, snapshot-derived, view-only `View in 3D` modal; server-rendered share viewer links to `/gallery/:slug`; pure `resolveChip3DRenderMode` falls back to the static poster when WebGL is absent or the chip exceeds `CHIP_3D_PIECE_BUDGET` (400). No schema/migration/API/upload change; Three stays code-split out of the gallery initial path. Final gates: client 89 files/455 tests, server 62 files/243 tests, build/typecheck/lint green. Spec: `docs/superpowers/specs/2026-06-19-v7-m5-gallery-share-integration-design.md`; plan: `docs/superpowers/plans/2026-06-19-v7-m5-gallery-share-integration.md`.
- **V7-M6 Final QA & Release**: ✅ done — closing v7 milestone. Full regression green (client 89 files/455 tests, server 62 files/243 tests, build/server typecheck/lint). 3D/video QA recorded in `docs/ops/3d-showcase-qa.md`: agent-automated (Playwright MCP, real ANGLE-Metal WebGL) the gallery `View in 3D` (PBR render, view-only, turntable), editor showcase (hexagon + circle neon-fantasy glow/bloom, `Export turntable MP4` present), share link target, and the WebGL-off → poster fallback across rect/square/circle/hexagon + neon/mono; MP4 export + the M0-reference visual-quality sign-off are owner-manual (MP4 already concretely decoded in M3). Bundle: Three + recorder + `mp4-muxer` only in the lazy `chip3dStage-*` chunk (559.22 kB / 142.46 kB gzip via the `Chip3DViewer-*` wrapper), `core index clean of three`. Version line bumped to **0.5 v7** (README; `package.json` left at 1.0.0). `.gitignore` hardened against stray root-level images. Branch left unmerged for the owner's integration/PR decision. Spec: `docs/superpowers/specs/2026-06-19-v7-m6-final-qa-release-design.md`; plan: `docs/superpowers/plans/2026-06-19-v7-m6-final-qa-release.md`.

### v6 Mobile/Responsive (✅ complete through M4 — branch `v6-mobile-responsive`, not yet merged)

First responsive pass over the desktop-first app. No DB schema change, no migration, no new API; the breakpoint is **768px** and the only product code is responsive CSS + a few small component branches. Local-first, the export raster contract, and `src/domain/` purity are all untouched. Admin (`/admin`) stays **desktop-only and out of scope**. Spec: `docs/superpowers/specs/2026-06-17-v6-mobile-responsive-design.md`; roadmap: `docs/superpowers/specs/2026-06-16-v5-v6-roadmap-design.md`.

- **V6-M0 Responsive Foundation**: ✅ done — removes the desktop-only viewport floor; canonical 768px breakpoint in one pure module (`src/lib/breakpoints.ts`) + a `useIsMobile()` `matchMedia` hook; the header nav becomes a CSS-driven hamburger **drawer** (open on tap, close on link/Escape/backdrop, auto-close + restore horizontal nav on resize to desktop). Plan: `docs/superpowers/plans/2026-06-17-v6-m0-responsive-foundation.md`.
- **V6-M1 Public Read Surfaces**: ✅ done — pure-CSS single-column reflow (no JS, no component changes) for landing `/`, gallery list `/gallery`, gallery detail `/gallery/:slug`, public profile `/u/:handle`, and the server-rendered share viewer `/s/:slug` (responsive CSS injected into the share template `BASE_STYLE`). Plan: `docs/superpowers/plans/2026-06-18-v6-m1-public-read-surfaces.md`.
- **V6-M2 Account & Dashboard**: ✅ done — CSS reflow for the dashboard (preset + project grids single-column) and a gutter tweak for the Tailwind-based account page (`/account` + verify/reset/forgot states) and onboarding/first-run guidance. Plan: `docs/superpowers/plans/2026-06-18-v6-m2-account-dashboard.md`.
- **V6-M3 Editor Read-Only Mobile Preview**: ✅ done — on mobile, `EditorRoute` branches on `useIsMobile()` **before** mounting `EditorPage` (so editor hooks never mount; Rules-of-Hooks respected) into a new `MobileEditorPreview`: display-only Konva stage reusing the shared `ChipArtwork` render fit-to-width, fake spec, `PublishPanel` + `ExportPanel`, and an "Edit on desktop" CTA. Desktop authoring unchanged. Plan: `docs/superpowers/plans/2026-06-18-v6-m3-editor-mobile-preview.md`.
- **V6-M4 Mobile QA & Release**: ✅ done — full Playwright device-emulation QA matrix at 360/390px across every surface (desktop spot-check at 1280px), recorded in `docs/ops/mobile-qa.md`; one nit found + fixed (full-width comment form on mobile gallery detail); visual gate signed off (intentional/premium, no horizontal scroll, no clipped posters, drawer nav verified); launch QA checklist mobile line flipped to assessed; version bumped to 0.4 (v6). No release blockers. Plan: `docs/superpowers/plans/2026-06-18-v6-m4-mobile-qa-release.md`.

### v5 Public Launch (launch-ready checkpoint — branch `v5-public-launch`, gate not live)

- **V5-M0 Invite-Code Access**: ✅ done — replaces boolean signup gate with `VSL_ACCESS_MODE` (`closed`/`invite`/`open`), `008_invite_codes`, admin invite-code API, signup invite redemption, `/api/health` access mode, and authStore access-mode contract.
- **V5-M1 Safety & Moderation Hardening**: ✅ done — `009_safety` adds bans, comment hide fields, comment reports, and append-only audit log; admin routes cover ban/unban, comment queue/hide, chip feature/unfeature, report resolve/dismiss, and audit list.
- **V5-M2 Account Security**: ✅ done — `010_account_security`, email verification, forgot/reset password, verification-aware publish/reaction soft gates, reset session revocation, and account UI routes.
- **V5-M3 Onboarding & First-Run**: ✅ done — local first-run checklist and onboarding surface, featured gallery API + admin feature controls, Featured row in gallery, and dashboard/account guidance.
- **V5-M4 Discovery, SEO & Profiles**: ✅ done — `011_profiles`, public handles, `/u/:handle` client route, `/api/profiles/:handle`, `robots.txt`, and `sitemap.xml` for public share/profile URLs only.
- **V5-M5 Launch Hardening & Ops**: ✅ done — grouped gallery count query, rate-limit pruning + sensitive endpoint overrides, password reset contract fix, login timing dummy hash, safer publish image I/O, `VSL_GALLERY_LOCKDOWN`, SQLite backup helper/script, and backup/restore runbook.
- **V5-M6 Launch QA & Gate Flip**: ✅ checkpoint done, production flip pending — `server/test/launchFlow.test.ts` covers invite → verify → publish → moderate → ban → profile/SEO → reset; full gates are green (`npm test`, `npm run build`, server typecheck, lint); load smoke is recorded in `docs/ops/launch-runbook.md`; `docs/ops/launch-qa-checklist.md` tracks manual browser and production sign-off. Actual `VSL_ACCESS_MODE=invite` production change remains blocked on explicit owner go/no-go.

### v3 Share Core (✅ complete, merged to `main` — spec: `docs/superpowers/specs/2026-06-12-v3-v4-roadmap-design.md`)

- **V3-M0 Workspace & Server Skeleton**: ✅ done — npm workspaces conversion, Hono + better-sqlite3 server skeleton, transaction-safe migration runner (duplicate-id guard, empty production list until M1), `/api/health` reporting the shared domain `CURRENT_SCHEMA_VERSION`, shared-domain smoke tests pinning `migrateProject` as the publish validation entry; server suite 4 files / 11 tests.
- **V3-M1 Accounts**: ✅ done — `001_accounts` migration (users + sessions, FK cascade), argon2id (`@node-rs/argon2`, OWASP m=19456/t=2/p=1), signed `vsl_session` cookie carrying a raw token whose sha256 lives in SQLite (30-day TTL, lazy expiry), full account CRUD API (`/api/auth/signup·login·logout`, `GET/PATCH/DELETE /api/me`) with `{ error: { code, message } }` contract, password change revokes other sessions; client adds `/api` dev proxy, 4-state `authStore` (`offline` = normal, incl. 502/503/504 gateway mapping), `/account` page + header link themed via `--v2-*` vars. Suites: client 62 files / 296 tests, server 12 files / 54 tests. Plan: `docs/superpowers/plans/2026-06-12-v3-m1-accounts.md`.
- **V3-M2 Publish Pipeline**: ✅ done — `002_published_chips` migration, shared-domain snapshot validation, authenticated publish API (`POST /api/published-chips`, `GET/PATCH/DELETE /api/published-chips/source/:sourceProjectId`), republish version bumps, public/private toggle, unpublish, client `publishApi`, and editor `PublishPanel` using existing die/poster export stages. M2 stores PNG data URLs in SQLite TEXT to lock the API/UX first; M6 owns moving PNG storage to deploy-ready files plus upload limits.
- **V3-M3 Public Gallery**: ✅ done — unauthenticated public-only gallery API (`GET /api/gallery`, `GET /api/gallery/:slug`) hiding private/unpublished chips, poster-first `/gallery` grid, `/gallery/:slug` detail with poster + fake spec, header Gallery link, and detail page-theme application for hero-set snapshots.
- **V3-M4 Share Links**: ✅ done — server-rendered `/s/:slug` viewer (public chips only, reusing M3's `getPublicPublishedChipBySlug`) with escaped OG/Twitter meta + a `/s/:slug/poster.png` byte endpoint decoding stored poster data URLs for crawler previews, absolute URLs via `VSL_PUBLIC_BASE_URL` or request origin, public-only `shareUrl` on the publish API, and a PublishPanel copy-link control. New `server/src/share/` module keeps HTML rendering out of the JSON API boundary. Plan: `docs/superpowers/plans/2026-06-13-v3-m4-share-links.md`.
- **V3-M5 Remix Import**: ✅ done — pure `importRemixedProject` (migrate-on-import via `migrateProject` + independent `structuredClone`, fresh id/`{name} Remix`/timestamps, no schema change), `projectStore.remixImport`, a "Remix into my projects" button on the gallery detail page wired by App to `remixImport` + editor navigation, and the share viewer's "Remix this chip" CTA now linking to `/gallery/:slug`. No new server endpoint (gallery detail already returns the snapshot); lineage/provenance deferred to v4. Plan: `docs/superpowers/plans/2026-06-13-v3-m5-remix-import.md`.
- **V3-M6 Deploy Packaging & QA**: ✅ done — production runtime config requires `VSL_SESSION_SECRET` (32+ chars) + http(s) `VSL_PUBLIC_BASE_URL`, production enables Secure cookies and default mutating API rate limits, publish PNG data URLs are bounded by decoded-byte upload limits, and new publishes store PNG bytes in `VSL_UPLOAD_DIR` file storage with DB path columns plus legacy data URL dual-read. Added `/uploads/*` serving, deploy scripts/docs, and final browser QA for signup → publish → gallery → share → remix import → local edit. Plan: `docs/superpowers/plans/2026-06-13-v3-m6-deploy-packaging-qa.md`.
- v4 "Community" (moderation, reactions, ranking, contests, remix lineage) is direction-approved in the same spec; detailed design happens per-milestone after v3.

### v4 Community (in progress — branch `v4-community`, not yet merged)

- **V4-M0 Moderation + Access Gate**: ✅ done — first v4 milestone, adds pre-public-launch safety on the v3 server. Access gate `VSL_SIGNUPS_OPEN` (config default `false`; signup → `403 SIGNUPS_CLOSED` when closed, login/gallery/share unaffected; `/api/health` exposes `signupsOpen`). Env admin role `VSL_ADMIN_EMAILS` (runtime-derived via pure `isAdminEmail`, no `users.role`; `GET /api/me` returns `isAdmin`). `004_moderation` migration adds `moderation_status`/`hidden_*` to `published_chips` + a `reports` table; public gallery/share queries filter `moderation_status = 'visible'`. New `server/src/moderation/` module: service (`createReport`/`listReports`/`resolveReport`/`hideChip`/`unhideChip`/`adminDeleteChip`/`listChipsForModeration`) + routes (user `POST /api/reports`; admin-only `/api/admin/*` behind a `/admin/*` guard middleware). Client: authStore tracks `isAdmin`/`signupsOpen`, `/admin` moderation page + admin-only nav link, signup form replaced by a closed-beta notice when signups are closed (login stays). Hide=reversible, delete=permanent (owner's local project untouched, local-first preserved). **Deferred to M1+:** user-facing report button, likes, comments, user ban, invite codes, gallery-wide lockdown, audit-log table. Suites: client 69 files / 339 tests, server 28 files / 128 tests. Spec: `docs/superpowers/specs/2026-06-14-v4-m0-moderation-access-gate-design.md`; plan: `docs/superpowers/plans/2026-06-14-v4-m0-moderation-access-gate.md`.
- **V4-M1 Reactions (likes + comments + report button)**: ✅ done — first community interaction on M0's infra. `005_reactions` migration adds `likes` (composite PK `(published_chip_id, user_id)` = one like per user per chip) + `comments` (flat) tables, both CASCADE on chip/user delete; the report button reuses M0's `reports` table (no new table). New `server/src/reactions/` module: service (`isChipReactable`/`likeChip`/`unlikeChip`/`getLikeState`; `createComment`/`listComments`/`getCommentMeta`/`deleteComment`) + routes mounted at `/api` (likes `POST/DELETE /api/published-chips/:id/like`, comments `GET` public · `POST` auth · `DELETE` author-or-admin; auth via M0 `getSessionUser`, admin via `isAdminEmail`). Likes are idempotent (`INSERT OR IGNORE`); only `is_public=1 AND moderation_status='visible'` chips are reactable (else 404). Gallery queries expose `likeCount`/`commentCount`; gallery detail reads an optional session for `likedByMe`. Client: `reactionsApi` (+ `reportChip` reusing M0 reports), gallery detail page gets a ♥ like toggle, a report button (→ "Reported"), and a flat comment thread (author/admin Delete), grid cards show like count. Server-side only — local-first untouched. **Deferred to M2+:** comment reporting/threading/edit, comment hide, admin comment queue. Suites: client 70 files / 342 tests, server 32 files / 144 tests. Spec: `docs/superpowers/specs/2026-06-14-v4-m1-reactions-design.md`; plan: `docs/superpowers/plans/2026-06-14-v4-m1-reactions.md`.
- **V4-M2 Ranking/Trending**: ✅ done — read-side gallery sorting with no migration, reusing M1 `likes.created_at`/`comments.created_at`. `listPublicPublishedChips(db, { sort, now, limit })` supports `trending` (last-7-day `likes + comments`, inclusive cutoff), `top` (all-time `likes + comments`), and `newest` (`updated_at DESC`) with recency tie-breaks and visible/public filters preserved. `GET /api/gallery?sort=` is lenient (unknown/default → `trending`), `galleryApi.list(sort?)` forwards the param, and GalleryPage adds a `Trending`/`Top`/`Newest` segmented control with `aria-pressed`. Suites: client 70 files / 344 tests, server 33 files / 150 tests. Spec: `docs/superpowers/specs/2026-06-15-v4-m2-ranking-design.md`; plan: `docs/superpowers/plans/2026-06-15-v4-m2-ranking.md`.
- **V4-M3 Contests**: ✅ done — admin-run chip-design contests with `draft` → `submission` → `voting` → `results`, no scheduler/dates. `006_contests` migration adds `contests`, `contest_entries`, and `contest_votes`; `server/src/contests/` owns CRUD, entries, votes, phase checks, self-vote blocking, replace-on-conflict votes, and read-side results ranking. New endpoints: admin `/api/admin/contests`, public `/api/contests` and `/api/contests/:id`, user `/api/contests/:id/entries` and `/api/contests/:id/vote`; `GET /api/published-chips/mine` feeds the entry picker. Admin guards are inline per handler to avoid overlap with the moderation `/admin/*` sub-app. Client: `/contests`, `/contests/:id`, phase-aware entry/vote/results podium UI, and AdminPage contest create/status/delete panel. Suites: client 72 files / 352 tests, server 36 files / 173 tests. Spec: `docs/superpowers/specs/2026-06-15-v4-m3-contests-design.md`; plan: `docs/superpowers/plans/2026-06-15-v4-m3-contests.md`.
- **V4-M4 Remix Lineage**: ✅ done — first v4 schema bump from v4→v5 via optional `Project.remixedFrom` (`RemixOrigin` carries server `chipId` plus `slug`/`title` display metadata). `007_remix_lineage` adds nullable self-referencing `published_chips.remixed_from_chip_id` with `ON DELETE SET NULL`; publish resolves known parent ids and stores `NULL` for missing parents. `getChipLineage` derives root-first ancestors and direct public+visible children read-side; private/hidden ancestors become a non-clickable placeholder. New endpoint: `GET /api/gallery/:slug/lineage`; share viewer renders a direct-parent "Remixed from" link; gallery detail lazy-loads an ancestor spine and child grid. No materialized cache. Suites: client 73 files / 362 tests, server 38 files / 184 tests; `npm run build` and `npm run typecheck --workspace server` green. Browser QA covered A→B→C lineage, direct child count, private ancestor placeholder, parent delete/`SET NULL`, share viewer parent link, and server-down local editor entry. Spec: `docs/superpowers/specs/2026-06-15-v4-m4-remix-lineage-design.md`; plan: `docs/superpowers/plans/2026-06-15-v4-m4-remix-lineage.md`. This is the last planned v4 milestone on `v4-community`; public launch remains a separate gate.

### SoC Custom Studio (post-v2, on `main`)

- ✅ done — studio kit UI/inspector, schema v2 studio data model + migration, deterministic global reflow engine, generated fake-spec engine with component-level estimates, three-zone editor redesign. Specs: `docs/superpowers/specs/2026-06-07-soc-custom-studio-design.md`, `2026-06-09-editor-reference-fidelity-design.md`; decisions in `implementation.md`. Test suite at this point: 58 files / 267 tests.

### v2 Visual Major

- **V2-M0 Visual Audit & Direction**: ✅ done — reference audit, style direction, and 10 hero set targets in `docs/reference/`.
- **V2-M1 Page Theme System & App Shell**: ✅ done — `laboratory`/`anime`/`space`, app root CSS variables, theme switcher, landing/dashboard redesign.
- **V2-M2 Editor Chrome Redesign**: ✅ done — three-zone desktop tool surface, product analysis stage, segmented toolbar, export rail.
- **V2-M3 Chip Material Renderer**: ✅ done — material recipes, chip layer model, package/micro/trace/readout/glow rendering shared by editor/export.
- **V2-M4 Poster Export Redesign**: ✅ done — three poster compositions with the same `3200x1800` output contract.
- **V2-M5 Hero Sets & Random Generator**: ✅ done — 10 v2 hero presets, deterministic non-AI random chip generation, dashboard integration.
- **V2-M6 Final QA & Release Pack**: ✅ done — `npm test` 39 files / 146 tests, `npm run build` green with known chunk warning, desktop Browser QA passed for landing/theme/dashboard/editor/random/export controls, 150-block layer smoke covered by test.

### v1 MVP

- **M0 Reference Board** (no code): ✅ done — `docs/reference/` board (`README.md` three named directions + global EDA anti-reference, `visual-direction.md` per-theme Konva tokens, `hero-compositions.md` first-hero comps A/B/C). M3 visual work and the first hero chip are reviewed against this board, not ad-hoc taste.
- **M1 Foundation Vertical Slice**: ✅ done — project CRUD, IndexedDB+fallback, rectangular-die editor, bounded drag, refresh persistence.
- **M2 Editor Core**: ✅ done (`docs/superpowers/plans/2026-06-02-editor-core.md`) — four die shapes with bounded drag/resize, zoom/pan/grid/snap, resize/rotate via Konva Transformer, single selection, undo/redo/delete/duplicate/reorder, keyboard shortcuts, debounced autosave. All commands unit-tested; browser-verified (Chrome/Playwright). Engine lives in `src/stores/editorStore.ts` + `src/features/editor/canvas/geometry.ts`.
- **M3 Visual System**: ✅ done (`docs/superpowers/plans/2026-06-02-visual-system.md`) — pure `src/themes/` boundary (token catalog for all five themes + gradient builders + style resolvers), theme-driven `ChipStage` (die/grid/blocks/glow + procedural memory texture), decoration rendering (neon line/warning/label/sci-fi, additive blend), `createHeroChip` (composition A) loadable from the dashboard, and a minimal `stage.toDataURL` PNG export. All three gates browser-verified (Chrome/Playwright): theme switch recolors the whole die; the AURORA C-1 hero chip reviewed against the M0 board; the exported PNG proves effects render in Konva (not DOM/CSS). Theme is the render-time source of truth (no migration); `die.background` is reserved for presets.
- **M4 Presets/Remix**: ✅ done (`docs/superpowers/plans/2026-06-02-presets-and-remixing.md`) — six immutable curated blueprints materialize into fresh ordinary `Project` JSON; `projectStore.remixPreset()` persists independent editable local projects; dashboard shows lightweight CSS summary cards instead of six live Konva stages. Browser-verified: blank start remains; AURORA keynote, N-9 neon hex, and M-7 matte military open in the editor; an edited Mono N-9 survives refresh while a fresh N-9 remix still starts Neon, proving source immutability.
- **M5 Fake Specs + Dual PNG Export**: ✅ done (`docs/superpowers/plans/2026-06-02-specs-and-export.md`) — shared `ChipArtwork` (die/grid/blocks/textures/labels/decorations) drives the editor and two dedicated offscreen Konva export stages; dynamic editor-stage sizing (no preset clipping); autosave flush on teardown; complete block-level z-order; editable fake-spec form + three bundled examples (`editorStore.setSpec`); die-only PNG at `pixelRatio:4` (exact die px ×4) and keynote poster PNG (logical `1600x900` @`pixelRatio:2` → `3200x1800`); Web Share with download fallback. No schema change (reuses `FakeSpec`). Browser-verified (Chrome/Playwright): raster dims confirmed via `sips` (AURORA die 2880², M-7 die 3680×2400, poster 3200×1800); posters carry full artwork + spec typography and **no editor controls**; AURORA/N-9/M-7 posters stay distinct; share falls back to download when `navigator.share` is unavailable. Task 0 also cleared standing M0–M4 nits (glow follows `colorOverride`; em-dash preset names; removed dead `createHero`).
- **M6 Landing/QA/Deploy**: ✅ done (`docs/superpowers/plans/2026-06-02-landing-and-release.md`) — release-hardening debt closed (rotation-aware rect/square clamp; full 16-type `BlockPalette`), direct-start landing page at `/`, project dashboard at `/dashboard`, dashboard polish, README, Netlify SPA fallback config, demo GIF placeholder, final desktop Chrome QA, and final code review. Chrome QA used headless Google Chrome via CDP: first block in 186ms, refresh persistence verified, AURORA poster downloaded at 3200x1800, 150-block smoke completed, no app console errors beyond favicon 404. `npm run build` passes with the known Vite chunk warning. Final review found no release blockers.
- **Pre-merge review fixes**: ✅ done (`docs/superpowers/plans/2026-06-03-pre-merge-review-fixes.md`) — a full-branch review's Important findings were resolved: missing `/editor/:id` shows a not-found view instead of looping on "Loading project…"; persistence validates project shape, skips corrupt records in `list()`, and sticks to the localStorage fallback after a primary failure; poster share guards malformed data URLs and a cancelled share; decorations are documented as an intentional top overlay. `npm test` = 30 files / 112 tests; `npm run build` green. Remaining minor items are backlogged in that plan. The production bundle still exceeds Vite's 500 kB warning (post-MVP code-split candidate).

## Merge Status

- v1 MVP: ✅ merged into `main` via fast-forward (commit `bac1d8e`).
- v2 visual major: ✅ merged into `main` (linear commits, capped by `9e35f2f` "0.1_v2_prototype_complete"); the `v2-m2-editor-redesign` branch no longer exists.
- SoC Custom Studio: ✅ on `main` (same linear history).
- v3 Share Core: ✅ merged into `main` via GitHub PR #1 (merge commit `f978e51`, 2026-06-14); the `v3-share-core` branch has been deleted (remote + local).

> Visual-quality gate: do not advance past M3 if glow/neon looks amateurish; the first hero chip is manually reviewed against the M0 reference board.

## Document Map

- `README.md` — project overview, features, dev/deploy, docs index, reference board summary, and export QA flow.
- `docs/spec-v1.md` — product/requirements spec (v1 / post-MVP / excluded) **[Korean]**.
- `docs/spec-v2.md` — v2 visual major scope, design direction, and decisions **[Korean]**.
- `docs/reference/` — visual reference board: per-theme tokens, hero compositions, v2 audit/style/hero-set notes.
- `docs/superpowers/plans/` — milestone roadmap + condensed per-milestone plan summaries (goal, decisions, outcome). Full bite-sized TDD steps remain in git history.
- `docs/superpowers/specs/` — design specs for in-progress features.
- `implementation.md` — condensed running log of per-milestone decisions and outcomes **[Korean]**.

## Explicitly Out Of Scope / Deferred to v5+

Mobile 3D viewer/editor · advanced 3D materials/animation · AI prompt generation · payments/monetization ·
custom freeform die paths · PixiJS shaders · animation simulation · worldbuilding pages ·
MP4 export · two-way sync / multi-device editing · GDSII/DRC/LVS/manufacturing compatibility.

v3 covers backend + SQLite, accounts, publish/gallery/share links, remix import (deploy-ready, no
public launch). v4 covers moderation, reactions, ranking, contests, remix lineage; a standalone
text board was explicitly rejected (absorbed into per-chip comments + contest announcements).
Public launch is a separate gate decided at v4 start.
